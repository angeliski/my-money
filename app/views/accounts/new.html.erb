<div class="container mx-auto px-4 py-6">
  <div class="mb-6">
    <%= link_to t('accounts.actions.back_to_list'), accounts_path, class: "text-cyan-400 hover:text-cyan-300 text-sm" %>
  </div>

  <div class="bg-slate-800 border border-slate-700 rounded-lg p-8 max-w-2xl">
    <h1 class="text-2xl font-bold text-white mb-6"><%= t('.title') %></h1>
    <%= render partial: "accounts/form", locals: { account: @account } %>
  </div>
</div>
