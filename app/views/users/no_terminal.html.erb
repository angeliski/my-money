<div class="min-h-screen bg-slate-800">
  <!-- Header -->
  <header class="bg-slate-900 border-b border-slate-700 px-4 py-4 sticky top-0 z-40">
    <div class="flex items-center justify-between max-w-7xl mx-auto">
      <div class="flex items-center gap-3">
        <svg class="w-8 h-8 text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
        </svg>
        <div>
          <h1 class="text-xl font-bold text-white">Sem Terminal Associado</h1>
          <p class="text-xs text-gray-400">Aguardando Configuração</p>
        </div>
      </div>
      <%= link_to destroy_user_session_path, method: :delete, class: "bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg flex items-center gap-2 transition" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
        </svg>
        <span class="hidden sm:inline">Sair</span>
      <% end %>
    </div>
  </header>

  <!-- Main Content -->
  <div class="max-w-4xl mx-auto p-4 sm:p-6 lg:p-8">
    <div class="flex items-center justify-center min-h-[calc(100vh-200px)]">
      <div class="w-full">
        <div class="bg-slate-900 border border-slate-700 rounded-lg p-8 text-center">
          <!-- Icon -->
          <div class="mb-6 flex justify-center">
            <div class="rounded-full bg-yellow-500/20 p-6">
              <svg class="h-16 w-16 text-yellow-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
              </svg>
            </div>
          </div>

          <!-- Title -->
          <h2 class="text-3xl font-bold text-white mb-4">
            Nenhum Terminal Associado
          </h2>

          <!-- Message -->
          <div class="space-y-4 mb-8">
            <p class="text-lg text-gray-300">
              Você ainda não possui um terminal associado à sua conta.
            </p>
            <p class="text-gray-400">
              Para utilizar o sistema, é necessário que um administrador associe um terminal à sua conta.
            </p>
            <div class="bg-slate-800 border border-slate-700 rounded-md p-4 mt-6">
              <p class="text-sm text-gray-400">
                <strong class="text-white">O que fazer?</strong><br>
                Entre em contato com o administrador do sistema para solicitar a associação de um terminal.
              </p>
            </div>
          </div>

          <!-- User Info -->
          <div class="border-t border-slate-700 pt-6">
            <div class="text-sm text-gray-400">
              <p class="mb-2">
                <strong class="text-white">Usuário:</strong> <%= current_user.name %>
              </p>
              <p>
                <strong class="text-white">Email:</strong> <%= current_user.email %>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
